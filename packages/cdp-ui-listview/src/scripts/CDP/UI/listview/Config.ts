namespace CDP.UI {
    /**
     * @class ListViewGlobalConfig
     * @brief cdp.ui.listview の global confing
     */
    export module ListViewGlobalConfig {
        export let WRAPPER_CLASS = "listview-wrapper";
        export let WRAPPER_SELECTOR = "." + WRAPPER_CLASS;
        export let SCROLL_MAP_CLASS = "listview-scroll-map";
        export let SCROLL_MAP_SELECTOR = "." + SCROLL_MAP_CLASS;
        export let INACTIVE_CLASS = "inactive";
        export let INACTIVE_CLASS_SELECTOR = "." + INACTIVE_CLASS;
        export let RECYCLE_CLASS = "listview-recycle";
        export let RECYCLE_CLASS_SELECTOR = "." + RECYCLE_CLASS;
        export let LISTITEM_BASE_CLASS = "listview-item-base";
        export let LISTITEM_BASE_CLASS_SELECTOR = "." + LISTITEM_BASE_CLASS;
        export let DATA_PAGE_INDEX = "data-page-index";
        export let DATA_CONTAINER_INDEX = "data-container-index";
    }
}
